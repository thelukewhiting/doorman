<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Forward Mode</h3>
  </div>
  <div class="panel-body">
    <p>Incoming calls will be forwarded to:</p>
      <%= @settings.recipient %>
      <%= @settings.forward1 if @settings.forward1 != '' %>
      <%= @settings.forward2 if @settings.forward2 != '' %>
      <%= @settings.forward3 if @settings.forward3 != '' %>
      <%= @settings.forward4 if @settings.forward4 != '' %>
  </div>
</div>